MideaWallProject_001 에서 언리얼 파일을 실행합니다.

그리고 main.py 를 실행하여 미디어파이프기반 코드를 실행합니다.

main.py는 미디어파이프를 통해 이용자를 인식하는 코드입니다.
반드시 pc와 연결된 카메라가 있어야 합니다.

만약 카메라가 있어도 main.py가 카메라를 인식하지 못하면 카메라의 인덱스 넘버를 확인해야합니다.
debug.py 를 실행하면 연결된 카메라의 인덱스넘버를 확인할수있습니다.
확인된 인덱스넘버를  main.py의 19번째 줄    cap = cv2.VideoCapture(4, cv2.CAP_DSHOW) 에서 4를 카메라가 인식된 인덱스 넘버로 수정하고 실행하세요
